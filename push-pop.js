var numbers = [78, 45, 98, 45];

// something new value adding process in arrays. Use push() to add element to an array as the last element array
// numbers.push(63);
console.log(numbers);

var friends = ['balam', 'kalam', 'salam'];
friends.push('gelam');
friends.push('pailam');
console.log(friends);

// use pop to loss last element
numbers.pop();
console.log(numbers);

// way to know the pop element
var element = numbers.pop();
console.log(element);
